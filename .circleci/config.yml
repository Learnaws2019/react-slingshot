version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: npm i
      - run: npm run lint
      - run: echo "Hello Amit"
  test:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: npm i
      - run: npm run test
      - run: echo "Hello Amit"
  analyze:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: echo "Hello Amit"

workflows:
  my_workflow:
    jobs:
      - build
      - test:
          requires:
            - build
      - analyze:
          requires:
            - test